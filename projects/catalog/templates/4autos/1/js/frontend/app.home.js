(function ($, window, undefined) {
    $.app.home = $.extend({}, {
        slider: false,
        init: function ()
        {
   

            return true;
        }
        
    }, $.app.home);
})(jQuery, this);
